# 2026-09-19 — Optional OR tag matching

## Summary
- Confirmed that both commands already accepted multiple tags with AND matching;
  the initial investigation and planning left the repository unchanged.
- Added the shared `--or` flag to `fetch` and `sync`, retaining AND by default.
- Added keyword-only `match_any=False` to query building and API fetching. OR
  appends Raindrop's `match:OR` to nonempty queries before pagination begins.
- Updated CLI help and progress messages to explain the selected matching mode.
- Updated README usage examples, architecture documentation, and prompt history;
  added the required empty `AGENTS.md` (the existing `CLAUDE.md` was also empty).
- No dependencies or environment variables changed; no commit or push performed.

## Verification
- Added tests before implementing the feature and confirmed they failed because
  the flag, API parameter, and matching-mode output were not yet available.
- Full suite: `uv run pytest -v` — **34 passed**, run with offline mode and a
  temporary uv cache directory.
- New coverage includes AND/OR mode on both commands, collection and commit-message
  options, quoted tags containing spaces, single and empty tag lists, pagination,
  missing CLI tags, and sync's no-changes path.
- CLI tests mock HTTP and git effects, while saving URLs in temporary directories.
- Optional manual acceptance check: against a Raindrop collection with known
  overlapping tags, compare AND and OR results using `fetch` from a temporary
  directory so the repository's URL list stays unchanged.

## Suggested commit message
`feat: add optional OR tag matching to fetch and sync`
